# 技能配置（含 API key，勿提交）
scripts/vision_config.json
runtime/config/xunfei_config.json
vision_config.json
**/vision_config.json

# 运行时生成（logs、state、screenshots 统一在 runtime/）
runtime/
*.log

# 便携环境（体积大，用户自行 pip install）
python/

# 缓存与临时
__pycache__/
*.pyc
nul
get-pip.py
