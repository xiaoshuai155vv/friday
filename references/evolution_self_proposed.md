# 进化环自主提出的改进项（问 LLM 能做什么）

当 \ 无明确待办时，进化环在「假设」阶段**向 LLM 自问**：我能做什么？如何让星期五更**拟人、自主意识、自主决策、自主创新**？据此在此列出 1～3 条本轮回可执行改进，再在「决策」中选一条执行。格式：**简要描述 | 预期动作 | 状态**。

| 简要描述 | 预期动作 | 状态 |
|----------|----------|------|
| iHaier 场景交互流程优化：分析历史失败教训，改进 iHaier 相关 plan 的健壮性（如坐标偏移处理、窗口激活重试、剪贴板 fallback） | 1) 审查 iHaier 相关 plan 中的 vision_coords_offset 是否正确；2) 添加窗口激活失败的重试逻辑或 fallback；3) 验证并更新 plan | 已完成 |
| run_plan 错误处理增强：为 run_plan 添加重试机制，当遇到 vision 超时、窗口激活失败等情况时自动重试，提升脚本健壮性 | 1) 检查 run_plan.py 现有错误处理逻辑；2) 添加可选的重试参数（如 --max-retry、--retry-delay）；3) 对关键步骤（vision、click、activate）添加异常捕获与重试 | 已完成 |
| 增强脚本错误处理与日志可追溯性：分析 failures.md 中频繁出现的问题，为核心脚本（run_plan.py、do.py、vision_coords.py）增加更详细的错误日志和异常分类，便于问题溯源 | 1) 分析 failures.md 识别高频错误类型；2) 为关键脚本添加结构化错误日志（错误码+描述+上下文）；3) 在 behavior_log 中增加错误类型统计；4) 更新 self_verify_result.json 增加错误分类 | 已完成 |
| 增强私域知识自动加载和利用机制：让系统在执行任务时能自动识别并加载相关私域知识，提升任务执行的个性化和准确性 | 1) 修改 do.py 脚本，使其在执行任务时能自动加载相关的私域知识；2) 实现一个简单的私域知识缓存机制；3) 为系统添加私域知识的自动查询和应用能力 | 已完成 |
| 扩展场景计划库：增加看新闻、刷知乎等生活娱乐场景的自动化计划 | 1) 创建 assets/plans/read_news.json（看新闻场景）；2) 创建 assets/plans/browse_zhihu.json（刷知乎场景）；3) 按照现有 plan 格式定义 triggers 和 steps | 已完成 |
| 增强长期记忆与用户偏好学习机制：让系统跨会话记住用户习惯，提升任务执行的个性化程度 | 1) 创建 user_preferences.json 存储用户偏好；2) 修改 do.py 在执行任务时自动加载和利用用户偏好；3) 添加用户偏好自动学习功能（记录用户选择并应用到后续任务） | 已完成 |
| 主动状态自检与健康报告：让系统具备主动自检能力，定期检查核心功能状态，生成健康报告，增强自主意识 | 1) 创建 system_health_check.py 脚本检查核心能力状态；2) 修改 self_verify_capabilities.py 输出结构化健康报告；3) 将健康状态写入 runtime/state/health_report.json | 已完成 |
| 系统健康诊断与自动修复引擎：在健康检查基础上实现自动修复能力，对常见问题（磁盘满、内存高、进程卡死等）提供自动诊断与修复操作 | 1) 创建 auto_fixer.py 模块，实现常见问题的自动修复；2) 支持磁盘清理、进程清理、内存优化等修复操作；3) 集成到 do.py 支持关键词触发；4) 保存修复历史到 runtime/state/fix_history.json | 已完成 |
| 智能场景推荐机制：根据用户历史行为和偏好，智能推荐可能需要的场景计划，提升拟人化交互体验 | 1) 分析用户历史行为模式；2) 修改 do.py 添加场景推荐功能；3) 在用户意图不明确时推荐相关计划 | 已完成 |
| 增强主动状态推送与进度通知 | 1) 创建任务状态推送模块，将 run_plan 进度写入 runtime/state/task_status.json；2) 修改 run_plan.py 集成该模块 | 已完成
| 探索新场景计划：扩展场景计划库覆盖更多「用电脑」场景，如看电影、视频会议、文件批量处理等 | 1) 创建 assets/plans/watch_movie.json（看电影场景）；2) 创建 video_conference.json（视频会议场景）；3) 创建 batch_file_operation.json（文件批量处理场景） | 已完成 |
| 场景自动感知与主动建议：让系统能主动感知当前前台窗口/应用，自动推荐可能需要的场景计划（如检测到iHaier时主动推荐绩效申报、发送消息等），实现更像人的主动关怀交互 | 1) 实现前台窗口感知脚本 get_foreground_window.py；2) 创建场景匹配函数，根据前台应用匹配 assets/plans/ 下相关 plan；3) 修改 do.py 在空闲时或在用户模糊意图时主动推荐；4) 输出主动建议到 runtime/state/active_suggestions.json | 已完成 |
| 任务执行策略自适应机制：根据任务类型、历史执行情况、当前环境自动选择最佳执行策略（串行/重试/等待），提升任务执行的智能化程度和成功率 | 1) 创建 task_execution_strategy.py 模块；2) 实现根据步骤类型、环境信息、历史执行情况的策略选择；3) 集成到 run_plan.py 中在执行前获取策略并应用；4) 记录执行结果到策略历史 | 已完成 |
| 自动健康检查守护进程：支持定时自动执行系统健康检查并更新状态 | 1) 创建 health_check_daemon.py 守护进程脚本；2) 支持 --start/--once/--status/--history/--interval 参数；3) 集成到 do.py 支持关键词触发；4) 保存历史记录和状态文件 | 已完成 |
| 智能时间管理与专注提醒：实现番茄钟、定时休息提醒、专注模式，让系统像人一样提醒用户休息和保持专注 | 1) 创建 focus_reminder.py 模块，实现番茄钟(25+5分钟)、定时休息提醒、专注模式；2) 集成到 do.py 支持关键词触发；3) 与健康检查系统联动 | 已完成 |
| 意图智能识别与推荐系统：让系统能理解用户的模糊意图（如「有啥好玩的」「推荐个东西」），并根据当前状态、时间、用户习惯智能推荐合适的场景计划或行动 | 1) 创建 intent_recognition.py 模块，实现意图分类与智能推荐；2) 分析当前系统状态、时间、用户习惯生成推荐；3) 集成到 do.py 支持「推荐」「有啥好玩的」等模糊意图触发；4) 输出推荐结果到 runtime/state/intent_recommendations.json | 已完成 |
| 情感交互模块：让系统能识别用户情感状态（如疲惫、无聊、开心等）并给予情感化回应，增强拟人化交互体验 | 1) 创建 emotional_interaction.py 模块，实现情感识别与情感化回应；2) 支持识别用户输入中的情感倾向；3) 根据情感状态生成关心、鼓励、安慰等情感化回应；4) 集成到 do.py 支持情感交互 | 进行中 |
| 用户行为自动学习与适应：让系统能记住用户习惯并在后续任务中自动应用这些习惯，实现更个性化的服务 | 1) 创建 user_behavior_learner.py 模块，实现用户行为模式学习；2) 记录用户高频任务、时间段偏好、常用场景；3) 根据学习到的习惯自动调整推荐和执行策略；4) 集成到 do.py 支持用户行为学习功能 | 已完成 |

说明：
- **状态**：已完成 → 进行中（决策/执行阶段）→ 已完成（反思阶段更新）。
- 完成一项后可删或改为「已完成」并保留一行作记录；新轮次可追加新行。
- 来源与自问方式见 \ 中「无缺口时自主找事做：问 LLM 能做什么」。
